---
type: improved
---

**One key per file in a review.** `space` in the diff reader marks the file read and drops you on the next one you haven't read — so a 30-file PR is `space` until it runs out. `m` still toggles read/unread without moving, and `?` now lists every key in the reader.
