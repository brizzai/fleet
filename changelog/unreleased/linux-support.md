---
type: added
---
Linux support. `install.sh` now installs on Linux (x86_64/arm64), release builds ship Linux binaries, browser launches (PR open, bug-report URL) use `xdg-open`, and the Chrome native-messaging host installs under `~/.config/google-chrome/`.
