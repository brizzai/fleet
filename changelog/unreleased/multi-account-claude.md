---
type: added
highlight: true
---

**Two Claude subscriptions, one fleet.** Add a second account with `Ctrl+K` → "Manage Claude Accounts" and each new session runs on whichever account has more headroom, so one spent 5-hour window no longer stalls every session. A top-right readout alternates between each account's 5-hour and weekly usage.
