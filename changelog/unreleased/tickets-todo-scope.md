---
type: added
---

**Todo-only tickets.** `Ctrl+O` in the tickets tab (`t`) hides what you've already started, leaving just your todo and backlog. The tab chip reads `tickets · todo` while it's on.

**More of your backlog.** The tickets tab now fetches 100 issues per tracker instead of 50 — the cut was ordered by recency, so it was truncating exactly the untouched backlog.
