---
type: improved
---

**Review comments stay where you wrote them.** A comment now renders in the diff at the line it's about instead of vanishing into a counter, and `⇥` tags it `issue`, `nit`, `question` or `suggestion` — so you stop typing "nit:" by hand. `e` edits one, `d` deletes it, and they survive closing the reader.
