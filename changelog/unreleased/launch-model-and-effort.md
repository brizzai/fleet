---
type: added
---

**Pick the model per session** — `fleet add` and `fleet worktree` now take `--model` and `--effort`, so a session can start on `opus` at `xhigh` without touching your defaults. Works for all three agents.
