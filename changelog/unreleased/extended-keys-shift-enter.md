---
type: fixed
---
**Shift+Enter works now.** Fleet turns on tmux `extended-keys` for its sessions, so `Shift+Enter` inserts a newline in Claude Code instead of submitting the message. Set `FLEET_NO_EXTENDED_KEYS` to opt out.
