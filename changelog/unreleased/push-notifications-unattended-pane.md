---
type: fixed
---
**Phone notifications reach you again.** A `fleet send` or `Y` approval looked to Claude exactly like you typing at the pane, so it silently dropped every push with "Not sent because you're active in this terminal" — for the rest of that session. fleet now tells an unattended pane it has lost focus, so only sessions you are actually attached to stay quiet.
