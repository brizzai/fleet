---
type: fixed
---
**No more phantom suspends.** On Linux, idle-session suspend no longer fires on healthy machines with small or partly-used swap partitions — low free swap now only escalates when the kernel already reports memory stall. The worktree-delete dialog also shows tidy process names (`vite`, not a full path).
